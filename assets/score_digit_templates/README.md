Drop manually cropped single-digit Hardpoint score samples here.

Suggested workflow:
- collect clean single digits from the temporary Hardpoint score assets
- place each digit crop into the matching folder `0` through `9`
- use multiple examples per digit when possible

Recommended starting target:
- at least `3` examples per digit
- more for digits that look visually inconsistent
